import generateProgramManagerMetadata from "../generate_metadata";
import { ParamsArgument } from "../generate_metadata";

export const generateMetadata = generateProgramManagerMetadata("Communication Center");

export default async function Page({params}: ParamsArgument) {

}